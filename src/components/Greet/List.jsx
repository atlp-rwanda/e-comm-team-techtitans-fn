// import { useSelector } from 'react-redux';

const UserList = () => {
  //const users = useSelector((state) => state.users);
  return (
    <ul>
      <li>helloooo i am UserList</li>
    </ul>
  );
};

export default UserList;
